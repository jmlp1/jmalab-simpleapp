# .NET build output — regenerated by `dotnet build`/`dotnet publish`, never hand-edited
app/SimpleApp/bin/
app/SimpleApp/obj/
app/SimpleApp/publish/

# Zip packages created for manual `az webapp deploy` pushes (see README "Deploying app code changes")
*.zip
